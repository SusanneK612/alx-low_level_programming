#include "MAIN.H"

/**
 * main - check the code
 * Return: Always 0
 */
int main(void)
{
	char *str;

str = "I do not fear computers.I fear the lack the lack of them - Isaac Asimov";
	_puts(str);
	return (0);
}
